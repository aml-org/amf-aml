#%Dialect 1.0
dialect: SemEx
version: "1.0"
documents:
  root:
    encodes: MultipleEnums

nodeMappings:
  MultipleEnums:
    mapping:
      a:
        range: number
        enum:
          - 2.0
          - Pepito

