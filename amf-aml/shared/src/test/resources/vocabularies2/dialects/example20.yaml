#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  Local:
    classTerm: v3.Local
    mapping:
      a:
        patch: insert
        unique: true
        mandatory: true
        propertyTerm: v3.a
        range: string
  Declarations:
    classTerm: v3.Decla
    mapping:
      b:
        mandatory: true
        propertyTerm: v3.b
        range: string
  UnionNode:
    typeDiscriminatorName: lala
    typeDiscriminator:
      local: Local
      decls: Declarations
    union:
      - Local
      - Declarations
  Root:
    classTerm: v3.Root
    mapping:
      union:
        propertyTerm: v3.union
        range: UnionNode
documents:
  root:
    encodes: Root
    declares:
      nodes: UnionNode
external:
  v3: http://test.com/v3#
