#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  Local:
    classTerm: v3.Local
    mapping:
      a:
        patch: insert
        unique: true
        mandatory: true
        range: string
        propertyTerm: v3.a
documents:
  root:
    encodes: Local
  options:
    selfEncoded: true
external:
  v3: http://test.com/v3#
