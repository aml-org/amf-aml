#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  Local:
    classTerm: v3.Local
    mapping:
      a:
        patch: insert
        unique: true
        mandatory: true
        propertyTerm: v3.a
        range: string
documents:
  root:
    encodes: Local
  options:
    selfEncoded: true
external:
  v3: http://test.com/v3#
