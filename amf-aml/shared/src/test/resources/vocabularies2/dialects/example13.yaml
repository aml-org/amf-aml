#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  Local:
    classTerm: v3.Local
    mapping:
      za:
        range: lib.nodeB
        propertyTerm: v3.za
documents:
  root:
    encodes: Local
external:
  v3: http://test.com/v3#
uses:
  lib: mappings_lib.yaml
