#%Dialect 1.0
dialect: BooleanEnums
version: "1.0"
documents:
  root:
    encodes: MultipleEnums

nodeMappings:
  MultipleEnums:
    mapping:
      validEnum:
        range: boolean
        enum:
          - true
      invalidEnum:
        range: boolean
        enum:
          - true
