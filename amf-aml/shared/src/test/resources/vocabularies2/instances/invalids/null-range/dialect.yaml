#%Dialect 1.0
dialect: Test
version: 1.0
external:
  schema-org: http://schema-org.com

nodeMappings:
  otherMapping:
    classTerm: schema-org.other
  myNodeMapping:
    classTerm: schema-org.unreal
    mapping:
      myMapping:
        range: